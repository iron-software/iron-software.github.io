scaffolds/tools/** filter=lfs diff=lfs merge=lfs -text
# The Archetype-N tooling under scaffolds/tools/archetype-n/ is text (Python, JS,
# markdown prompts, and the overview cache) — keep it as normal git objects, not LFS.
scaffolds/tools/archetype-n/** !filter !diff !merge text=auto

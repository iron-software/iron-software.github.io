<!--
N-Lite/enum (IronPPT.Enums, base Enum). Members verified: Double, Simple, ThickThin, ThinThick, Triple.
Target: https://ironsoftware.com/csharp/ppt/object-reference/api/IronPPT.Enums.CompoundLineValues.html
-->

## Injected overview (Markdown)

`CompoundLineValues` sets how many parallel strokes make up a line or border. `Simple` is the single solid stroke and the usual choice, `Double` and `Triple` draw two or three equal parallel lines, and `ThickThin` and `ThinThick` pair a heavy stroke with a light one in the given order. Choose the value that matches the compound outline you want on a shape or border.

```csharp
var compound = CompoundLineValues.Simple;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `CompoundLineValues Enum - IronPPT C# API`
- v2 (human): `CompoundLineValues: Multi-Stroke Lines in C#`
- v3 (balanced): `CompoundLineValues Enum | IronPPT C# API`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set a compound line style in C# with the IronPPT CompoundLineValues enum: Simple, Double, Triple, ThickThin, and ThinThick strokes.`
- v2 (human): `Choose how many parallel strokes a line has in C# with the IronPPT CompoundLineValues enum: single Simple, Double, Triple, or paired.`
- v3 (balanced): `Reference for the IronPPT CompoundLineValues enum in C#: line stroke styles Simple, Double, Triple, ThickThin, and ThinThick.`

---

## Structured data

**TechArticle abstract**

> CompoundLineValues sets how many parallel strokes make up a line or border in IronPPT for C#. Simple is the single solid stroke, Double and Triple draw two or three equal parallel lines, and ThickThin and ThinThick pair a heavy stroke with a light one in the order named. Choose the value that matches the compound outline you want.

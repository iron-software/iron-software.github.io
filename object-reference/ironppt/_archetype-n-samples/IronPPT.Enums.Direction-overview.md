<!--
N-Lite/enum (IronPPT.Enums, base Enum). Members verified: LeftToRight, RightToLeft.
Target: https://ironsoftware.com/csharp/ppt/object-reference/api/IronPPT.Enums.Direction.html
-->

## Injected overview (Markdown)

`Direction` sets the reading direction text flows in. `LeftToRight` is the default for Latin scripts, and `RightToLeft` lays text out for languages such as Arabic and Hebrew. Set the value that matches the language of the content on a slide.

```csharp
var direction = Direction.LeftToRight;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `Direction Enum - IronPPT C# API Reference`
- v2 (human): `Direction: Text Reading Direction in C#`
- v3 (balanced): `Direction Enum | IronPPT C# API Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set the text reading direction in C# with the IronPPT Direction enum: LeftToRight for Latin scripts or RightToLeft for Arabic and Hebrew.`
- v2 (human): `Choose how text flows in C# with the IronPPT Direction enum: LeftToRight by default or RightToLeft for right-to-left languages.`
- v3 (balanced): `Reference for the IronPPT Direction enum in C#: text reading direction LeftToRight and RightToLeft for slide content.`

---

## Structured data

**TechArticle abstract**

> Direction sets the reading direction text flows in for IronPPT in C#. LeftToRight is the default for Latin scripts, and RightToLeft lays text out for languages such as Arabic and Hebrew. Set the value that matches the language of the content placed on a slide.

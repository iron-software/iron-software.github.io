<!--
N-Lite/enum. Members verified 2026-06-23: A4, A5, Letter, Legal, Custom. Base: Enum.
Target: https://ironsoftware.com/csharp/ppt/object-reference/api/IronPPT.Enums.PaperSize.html
-->

## Injected overview (Markdown)

Pick the physical page dimensions a slide or printed document uses with `PaperSize` in IronPPT. `Letter` and `Legal` cover the common North American sizes, `A4` and `A5` cover the ISO sizes used elsewhere, and `Custom` signals that explicit width and height values are supplied instead of a named size. Choose the value that matches how the deck will be printed or exported. The [add slide how-to](https://ironsoftware.com/csharp/ppt/how-to/add-slide/) walks through configuring a slide as you build the presentation.

```csharp
var size = PaperSize.A4;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PaperSize Enum - IronPPT C# API Reference`
- v2 (human): `PaperSize: A4, Letter, Legal in C# with IronPPT`
- v3 (balanced): `PaperSize Enum | IronPPT .NET API Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set page dimensions in C# with the IronPPT PaperSize enum: A4, A5, Letter, Legal, or Custom for explicit width and height values.`
- v2 (human): `Choose a slide or document page size in C# with the IronPPT PaperSize enum: Letter and Legal, A4 and A5, or Custom dimensions.`
- v3 (balanced): `Reference for the IronPPT PaperSize enum in C#: A4, A5, Letter, Legal, and Custom page-size values.`

---

## Structured data

**TechArticle abstract**

> Pick the page dimensions for a slide or printed document in IronPPT with PaperSize. Letter and Legal cover North American sizes, A4 and A5 cover the ISO sizes, and Custom signals that explicit width and height are supplied instead of a named size.

<!--
N-Lite/enum. Members verified 2026-06-23: Inline, Square, Tight, Behind, InFront.
Target: https://ironsoftware.com/csharp/ppt/object-reference/api/IronPPT.Enums.TextWrapStyle.html
-->

## Injected overview (Markdown)

`TextWrapStyle` describes how an object is positioned relative to the text around it in IronPPT. `Inline` keeps the object in the text flow as a character would sit, `Square` and `Tight` wrap text around the bounding box or contour, and `Behind` and `InFront` place the object on a separate layer beneath or above the text. Choose the layering and wrap that gives a slide the intended depth.

```csharp
var wrapStyle = TextWrapStyle.Square;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `TextWrapStyle Enum - IronPPT C# API Reference`
- v2 (human): `TextWrapStyle: Object Layering in C#`
- v3 (balanced): `TextWrapStyle Enum | IronPPT C# Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Position objects against text in C# with the IronPPT TextWrapStyle enum: Inline, Square, Tight, Behind, or InFront layering.`
- v2 (human): `Choose how an object layers with text in C# with the IronPPT TextWrapStyle enum: Inline, Square, Tight, Behind, or InFront.`
- v3 (balanced): `Reference for the IronPPT TextWrapStyle enum in C#: Inline, Square, Tight, Behind, and InFront object positioning.`

---

## Structured data

**TechArticle abstract**

> Position an object relative to surrounding text with IronPPT TextWrapStyle. Inline keeps it in the text flow, Square and Tight wrap text around it, and Behind and InFront place it on a separate layer beneath or above the text in C#.

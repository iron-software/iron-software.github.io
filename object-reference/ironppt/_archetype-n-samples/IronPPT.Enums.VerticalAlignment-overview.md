<!--
N-Lite/enum. Members verified 2026-06-23: Top, Middle, Bottom.
Sibling of VerticalAlign (same members) — different opener frame + disambiguation. Namespace IronPPT.Enums, IronPPT.dll.
Target: IronPPT.Enums.VerticalAlignment.html
-->

## Injected overview (Markdown)

`VerticalAlignment` sets where text or content rests inside the vertical bounds of its container, such as a table cell or text frame. `Top` aligns to the upper inside edge, `Middle` centers between the edges, and `Bottom` aligns to the lower edge. It mirrors the shorter `VerticalAlign` enum value-for-value; pick whichever name the property you are setting expects. The [add text how-to](https://ironsoftware.com/csharp/ppt/how-to/add-text/) covers placing text in a frame.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `VerticalAlignment Enum - IronPPT C# API`
- v2 (human): `VerticalAlignment: Vertical Text Anchor in C#`
- v3 (balanced): `VerticalAlignment Enum | IronPPT C# API`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Anchor content inside a container vertically in C# with the IronPPT VerticalAlignment enum: Top, Middle, or Bottom within its vertical bounds.`
- v2 (human): `Rest text at the top, middle, or bottom of a cell or frame in C# with the IronPPT VerticalAlignment enum: Top, Middle, or Bottom.`
- v3 (balanced): `Reference for the IronPPT VerticalAlignment enum in C#: Top, Middle, and Bottom vertical anchoring inside a container, mirroring VerticalAlign.`

---

## Structured data

**TechArticle abstract**

> Set where content rests inside the vertical bounds of a container in IronPPT with VerticalAlignment. Top aligns to the upper inside edge, Middle centers between the edges, and Bottom aligns to the lower edge. It mirrors the shorter VerticalAlign value-for-value, so pick whichever name the property expects.

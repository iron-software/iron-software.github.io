<!--
N-Lite/enum. Members verified 2026-06-23: Baseline, Subscript, Superscript.
Namespace IronPPT.Enums, IronPPT.dll. No concrete property consumer in api/ (described generically).
Target: IronPPT.Enums.VerticalPositionValues.html
-->

## Injected overview (Markdown)

Raising or lowering run text relative to the baseline uses `VerticalPositionValues`. `Baseline` is the default position that leaves text on the line, `Superscript` lifts it for exponents and footnote markers, and `Subscript` drops it for chemical formulae and indices. Apply it to a run when a character needs to sit above or below the surrounding text. The [add text how-to](https://ironsoftware.com/csharp/ppt/how-to/add-text/) covers run-level formatting.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `VerticalPositionValues Enum - IronPPT C# API`
- v2 (human): `VerticalPositionValues: Sub/Superscript in C#`
- v3 (balanced): `VerticalPositionValues | IronPPT C# API`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Raise or lower run text in C# with the IronPPT VerticalPositionValues enum: Baseline keeps text on the line, Superscript lifts it, Subscript drops it.`
- v2 (human): `Set superscript or subscript on slide text in C# with the IronPPT VerticalPositionValues enum: Baseline, Superscript, or Subscript.`
- v3 (balanced): `Reference for the IronPPT VerticalPositionValues enum in C#: Baseline, Superscript, and Subscript run positioning relative to the baseline.`

---

## Structured data

**TechArticle abstract**

> Raise or lower run text relative to the baseline in IronPPT with VerticalPositionValues. Baseline is the default that leaves text on the line, Superscript lifts it for exponents and footnote markers, and Subscript drops it for chemical formulae and indices. Apply it to a run that needs to sit above or below the surrounding text.

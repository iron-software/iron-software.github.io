<!--
N-Lite/enum. Members verified 2026-06-23: Flat, Round, Square. Base Enum.
Target: IronWord.Models.Enums.LineCapValues.html
-->

## Injected overview (Markdown)

Choose how the ends of a drawn line or stroke are finished with `LineCapValues`. `Flat` cuts the line off square at its exact endpoint, `Round` adds a semicircular cap that extends slightly past the end, and `Square` adds a squared cap that also projects beyond the endpoint. Pick `Round` or `Square` when a stroke needs a softer or fuller finish, and `Flat` for a precise stop.

```csharp
var lineCap = LineCapValues.Round;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `LineCapValues Enum - IronWord C# API Reference`
- v2 (human): `LineCapValues: Set Line End Caps in C#`
- v3 (balanced): `LineCapValues Enum | IronWord C# API Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set how line ends are finished in C# with the IronWord LineCapValues enum: Flat for a square stop, Round, or Square caps.`
- v2 (human): `Choose the cap on a drawn stroke in C# with the IronWord LineCapValues enum: Flat, Round, or Square line endings.`
- v3 (balanced): `Reference for the IronWord LineCapValues enum in C#: Flat ends a line square, Round and Square extend past the endpoint.`

---

## Structured data

**TechArticle abstract**

> Choose how the ends of a drawn line are finished with IronWord LineCapValues in C#. Flat cuts the line off square at its endpoint, Round adds a semicircular cap, and Square adds a squared cap that projects beyond the endpoint.

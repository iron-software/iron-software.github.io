<!--
N-Lite/enum. Members verified 2026-06-23: Point, Inch, Centimeter, Millimeter, Twip, EnglishMetricUnit.
No public consumer property found in this API surface; links 0.
Target: IronWord.Models.Enums.MeasurementUnit.html — base type Enum.
-->

## Injected overview (Markdown)

Express a size, offset, or distance in the unit a layout calls for by choosing a `MeasurementUnit`. `Point` is the typographic default used across most document measurements, `Inch`, `Centimeter`, and `Millimeter` cover the common physical units, `Twip` is the twentieth-of-a-point unit native to the underlying Word format, and `EnglishMetricUnit` is the fine-grained EMU used for drawing and image geometry.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `MeasurementUnit Enum - IronWord C# API Reference`
- v2 (human): `MeasurementUnit: Pick a Document Unit in C#`
- v3 (balanced): `MeasurementUnit Enum | IronWord C# Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Select the unit for IronWord measurements in C# with the MeasurementUnit enum: Point, Inch, Centimeter, Millimeter, Twip, or EnglishMetricUnit.`
- v2 (human): `Choose which unit IronWord uses for sizes and offsets in C# with MeasurementUnit: Point, Inch, Centimeter, Millimeter, Twip, or EMU.`
- v3 (balanced): `Reference for the IronWord MeasurementUnit enum in C#: Point, Inch, Centimeter, Millimeter, Twip, and EnglishMetricUnit values.`

---

## Structured data

**TechArticle abstract**

> Express a size or distance in the unit a layout needs with MeasurementUnit in IronWord. Point is the typographic default, Inch, Centimeter, and Millimeter cover physical units, Twip is the native Word unit, and EnglishMetricUnit is the fine-grained EMU for drawing geometry.

<!--
N-Lite/enum. Members verified 2026-06-23: Letter, A4, A5, Legal, Custom.
Consumer verified: PageSetup.PaperSize.
Target: IronWord.Models.Enums.PaperSize.html — base type Enum.
-->

## Injected overview (Markdown)

Choose the sheet a section is laid out for by assigning a `PaperSize` to `PageSetup.PaperSize`. `Letter` and `Legal` are the North American sizes, `A4` is the international standard for most business documents, `A5` is the smaller half-A4 sheet, and `Custom` lets you supply your own dimensions when none of the presets fit.

```csharp
var setup = new PageSetup { PaperSize = PaperSize.A4 };
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PaperSize Enum - IronWord C# API Reference`
- v2 (human): `PaperSize: Set the Page Size in C#`
- v3 (balanced): `PaperSize Enum | IronWord C# Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set the page size in C# with the IronWord PaperSize enum: Letter, A4, A5, Legal, or Custom, applied through PageSetup.PaperSize.`
- v2 (human): `Choose the sheet a Word section uses in C# with the IronWord PaperSize enum: Letter, Legal, A4, A5, or Custom dimensions.`
- v3 (balanced): `Reference for the IronWord PaperSize enum in C#: Letter, A4, A5, Legal, and Custom page sizes for PageSetup.PaperSize.`

---

## Structured data

**TechArticle abstract**

> Choose the sheet a section is laid out for with PaperSize in IronWord, assigned to PageSetup.PaperSize. Letter and Legal are the North American sizes, A4 is the international standard, A5 is the half-A4 sheet, and Custom supplies your own dimensions.

<!--
N-Lite/enum. Members verified 2026-06-23: None, Strike, DoubleStrike.
Target: https://ironsoftware.com/csharp/word/object-reference/api/IronWord.Models.Enums.StrikeValue.html
-->

## Injected overview (Markdown)

Apply a strikethrough line to run text by choosing a `StrikeValue` on the text style. `None` leaves the text unstruck and is the usual state, `Strike` draws a single line through the characters for crossed-out copy, and `DoubleStrike` draws two parallel lines for stronger emphasis. The [style text guide](https://ironsoftware.com/csharp/word/how-to/add-style-text/) shows strikethrough alongside the other character formatting options.

```csharp
textStyle.Strike = StrikeValue.Strike;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `StrikeValue Enum - IronWord C# API Reference`
- v2 (human): `StrikeValue: Strikethrough Word Text in C#`
- v3 (balanced): `StrikeValue Enum | IronWord C# Word API`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set strikethrough formatting in C# with the IronWord StrikeValue enum: None, Strike, or DoubleStrike for Word run text.`
- v2 (human): `Cross out text in C# with the IronWord StrikeValue enum: None for plain copy, Strike for a single line, DoubleStrike for two.`
- v3 (balanced): `Reference for the IronWord StrikeValue enum in C#: None, Strike, and DoubleStrike strikethrough options for run text.`

---

## Structured data

**TechArticle abstract**

> Apply a strikethrough line to run text in IronWord with the StrikeValue enum. None leaves text unstruck, Strike draws a single line through the characters, and DoubleStrike draws two parallel lines for stronger crossed-out emphasis on a text style.

<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.AspxToPdf.FileBehavior.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `FileBehavior` enumeration in IronPDF controls how a generated PDF is delivered to the browser when `AspxToPdf.RenderThisPageAsPdf` runs.

It belongs to the `IronPdf.AspxToPdf` namespace and has two values: `InBrowser` opens the PDF inline in a Chromium-class viewer, and `Attachment` triggers a download with an optional filename.

See [PDF from ASPX Pages](https://ironpdf.com/how-to/aspx-to-pdf/) for both delivery modes side by side.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `FileBehavior Enum - IronPDF C# API Reference`
- v2 (human): `FileBehavior: IronPDF ASPX-to-PDF Delivery in C#`
- v3 (balanced): `FileBehavior Enum | IronPDF C# ASPX to PDF`

**Meta-description (120-160 chars)**
- v1 (algorithm): `FileBehavior is the IronPDF enumeration for ASPX-to-PDF delivery in C#. Members InBrowser and Attachment select inline view or download.`
- v2 (human): `IronPDF FileBehavior enumeration for C#: controls whether RenderThisPageAsPdf streams the PDF inline or as a downloadable attachment.`
- v3 (balanced): `FileBehavior (ASPX to PDF) in IronPDF for C#: picks inline browser view or attachment download for the generated PDF. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The FileBehavior enumeration in IronPDF lives in the IronPdf.AspxToPdf namespace, derived from Enum, controlling how a generated PDF is delivered to the browser when AspxToPdf.RenderThisPageAsPdf runs. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

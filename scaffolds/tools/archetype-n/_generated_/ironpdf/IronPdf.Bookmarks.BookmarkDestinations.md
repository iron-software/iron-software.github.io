<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.Bookmarks.BookmarkDestinations.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `BookmarkDestinations` enumeration in IronPDF describes how a PDF bookmark targets its destination view when the reader jumps to a linked page.

Part of the `IronPdf.Bookmarks` namespace, its 8 values progress from the simplest `Page` (jump to a page) through coordinate-aware modes `PageX`, `PageY`, `PageZoom`, and `PageRect`, up to fit-to-view modes `PageBounds`, `PageBoundsX`, and `PageBoundsY` for automatic zoom.

Choose a value when adding hierarchical outlines. See [Outlines & Bookmarks](https://ironpdf.com/how-to/bookmarks/) for the full bookmarks workflow.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `BookmarkDestinations Enum - IronPDF C# API Reference`
- v2 (human): `BookmarkDestinations: IronPDF PDF Bookmarks in C#`
- v3 (balanced): `BookmarkDestinations Enum | IronPDF C# PDF Bookmarks`

**Meta-description (120-160 chars)**
- v1 (algorithm): `BookmarkDestinations is the IronPDF enumeration for PDF bookmarks in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF BookmarkDestinations enumeration reference for C#: bookmark destinations.`
- v3 (balanced): `BookmarkDestinations (PDF Bookmarks) in IronPDF for C#: bookmark destinations. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The BookmarkDestinations enumeration in IronPDF lives in the IronPdf.Bookmarks namespace, derived from Enum, representing bookmark destinations. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

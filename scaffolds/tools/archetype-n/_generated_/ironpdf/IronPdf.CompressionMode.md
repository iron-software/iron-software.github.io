<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.CompressionMode.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `CompressionMode` enumeration in IronPDF selects the compression strategy applied when a PDF is written to disk or stream.

Part of the `IronPdf` namespace, members are `Automatic`, `FastMemory`, and `HighQuality` (3 total). `AdvancedCompressionOptions` exposes this value alongside object-stream and image settings, so the choice trades runtime memory against output size on large documents.

See the [PDF compression guide](https://ironpdf.com/how-to/pdf-compression/) for tuning examples.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `CompressionMode Enum - IronPDF C# API Reference`
- v2 (human): `CompressionMode: IronPDF PDF Compression in C#`
- v3 (balanced): `CompressionMode Enum | IronPDF C# PDF Compression`

**Meta-description (120-160 chars)**
- v1 (algorithm): `CompressionMode is the IronPDF enumeration for selecting a PDF compression strategy in C#. Reference its members, declared values, and usage.`
- v2 (human): `IronPDF CompressionMode enumeration for C#: selects Automatic, FastMemory, or HighQuality when writing a PDF, trading memory for output size.`
- v3 (balanced): `CompressionMode in IronPDF for C# selects the PDF compression strategy (Automatic, FastMemory, HighQuality) used by AdvancedCompressionOptions.`

---

## Structured data

**TechArticle abstract**

> The CompressionMode enumeration in IronPDF lives in the IronPdf namespace, derived from Enum and specifies the compression strategy to use when reducing PDF file size. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

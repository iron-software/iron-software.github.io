<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.Editing.HorizontalAlignment.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `HorizontalAlignment` enumeration in IronPDF declares horizontal placement of stamps, watermarks, and overlay content relative to the page.

Part of the `IronPdf.Editing` namespace, it includes `Left`, `Center`, and `Right` (3 total).

Pair with `VerticalAlignment` when positioning a stamp on the canvas. See [Custom Watermarks](https://ironpdf.com/how-to/custom-watermark/) for the 3x3 placement grid used by `ApplyStamp` and `ApplyWatermark`.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `HorizontalAlignment Enum - IronPDF C# API Reference`
- v2 (human): `HorizontalAlignment: IronPDF PDF Editing in C#`
- v3 (balanced): `HorizontalAlignment Enum | IronPDF C# PDF Editing`

**Meta-description (120-160 chars)**
- v1 (algorithm): `HorizontalAlignment is the IronPDF enumeration for PDF editing in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF HorizontalAlignment enumeration reference for C#: the horizontal alignment relative to the page.`
- v3 (balanced): `HorizontalAlignment (PDF Editing) in IronPDF for C#: the horizontal alignment relative to the page. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The HorizontalAlignment enumeration in IronPDF lives in the IronPdf.Editing namespace, derived from Enum, representing horizontal alignment relative to the page. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.
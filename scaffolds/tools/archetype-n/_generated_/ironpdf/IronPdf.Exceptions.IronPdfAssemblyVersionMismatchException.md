<!--
GENERATED SAMPLE — Archetype N-Lite (lite/exception)
Target page: https://ironpdf.com/object-reference/api/IronPdf.Exceptions.IronPdfAssemblyVersionMismatchException.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `IronPdfAssemblyVersionMismatchException` class represents errors that occur while loading assemblies during IronPdf deployment.

Part of the `IronPdf.Exceptions` namespace and inheriting from `Exception`, it surfaces when the corresponding call cannot complete, typically from bad input, an unsupported configuration, or an error in the native library.

Inspect the `Message` and `InnerException` properties to diagnose what went wrong.

For common platform-specific causes, see the [ironpdf assembly version mismatch exception](https://ironpdf.com/troubleshooting/ironpdf-assembly-version-mismatch-exception/).

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `IronPdfAssemblyVersionMismatchException - IronPDF C#...`
- v2 (human): `IronPdfAssemblyVersionMismatchException: IronPDF Error...`
- v3 (balanced): `IronPdfAssemblyVersionMismatchException | IronPDF C#...`

**Meta-description (120-160 chars)**
- v1 (algorithm): `IronPdfAssemblyVersionMismatchException is the IronPDF exception class for error handling in C#. Reference its members, declared values, and usage in PDF...`
- v2 (human): `IronPDF IronPdfAssemblyVersionMismatchException exception class reference for C#: represents errors that occur while loading assemblies during IronPdf...`
- v3 (balanced): `IronPdfAssemblyVersionMismatchException (Error Handling) in IronPDF for C#: represents errors that occur while loading assemblies during IronPdf...`

---

## Structured data

**TechArticle abstract**

> The IronPdfAssemblyVersionMismatchException exception class in IronPDF lives in the IronPdf.Exceptions namespace, derived from Exception, representing errors that occur while loading assemblies during IronPdf deployment. Catch this exception at the boundary of the C# call that may raise it.

**Schema notes**
- Page type: `TechArticle` (lite/exception).
- No FAQPage entries for this variant per spec §5.
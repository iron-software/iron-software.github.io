<!--
GENERATED SAMPLE — Archetype N-Lite (lite/exception)
Target page: https://ironpdf.com/object-reference/api/IronPdf.Exceptions.IronPdfUnsupportedException.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `IronPdfUnsupportedException` class represents the error raised when attempting to use features not supported in the current environment or configuration.

It lives in the `IronPdf.Exceptions` namespace and extends `Exception`. The runtime throws it when the corresponding operation fails: invalid input, an unsupported configuration, or an error returned by the underlying library component.

Read the `Message` and `InnerException` properties to find the underlying cause.

Consult the [unsupported features](https://ironpdf.com/troubleshooting/unsupported-features/) for common platform-specific causes.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `IronPdfUnsupportedException - IronPDF C# API Reference`
- v2 (human): `IronPdfUnsupportedException: IronPDF Error Handling in C#`
- v3 (balanced): `IronPdfUnsupportedException | IronPDF C# Error Handling`

**Meta-description (120-160 chars)**
- v1 (algorithm): `IronPdfUnsupportedException is the IronPDF exception class for error handling in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF IronPdfUnsupportedException exception class reference for C#: thrown when attempting to use features not supported in the current...`
- v3 (balanced): `IronPdfUnsupportedException (Error Handling) in IronPDF for C#: thrown when attempting to use features not supported in the current... See members and usage.`

---

## Structured data

**TechArticle abstract**

> The IronPdfUnsupportedException exception class in IronPDF lives in the IronPdf.Exceptions namespace, derived from Exception, representing the error raised when attempting to use features not supported in the current environment or configuration. Catch this exception at the boundary of the C# call that may raise it.

**Schema notes**
- Page type: `TechArticle` (lite/exception).
- No FAQPage entries for this variant per spec §5.
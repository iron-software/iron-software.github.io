<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.Extractions.ExportFormat.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `ExportFormat` enumeration in IronPDF declares the 5 file targets that `ExportManager` and `ExportConfiguration` accept when serializing extracted PDF content.

Part of the `IronPdf.Extractions` namespace, it covers `Csv`, `Json`, and `Xml` for structured downstream pipelines, plus `Html` and `Txt` for human-readable output.

See [Extract Text & Images](https://ironpdf.com/how-to/extract-text-and-images/) for the `PdfExtractor` workflow these formats plug into.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `ExportFormat Enum - IronPDF C# API Reference`
- v2 (human): `ExportFormat: IronPDF Text Extraction in C#`
- v3 (balanced): `ExportFormat Enum | IronPDF C# Text Extraction`

**Meta-description (120-160 chars)**
- v1 (algorithm): `ExportFormat is the IronPDF enumeration for text extraction in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF ExportFormat enumeration reference for C#: supported export formats Defines the file formats available for exporting...`
- v3 (balanced): `ExportFormat (Text Extraction) in IronPDF for C#: supported export formats Defines the file formats available for exporting... See members and usage.`

---

## Structured data

**TechArticle abstract**

> The ExportFormat enumeration in IronPDF lives in the IronPdf.Extractions namespace, derived from Enum. Supported export formats Defines the file formats available for exporting extracted data. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.Extractions.TextExtractionMode.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `TextExtractionMode` enumeration in IronPDF picks the algorithm `PdfExtractor` uses to read characters from a PDF page.

Part of the `IronPdf.Extractions` namespace, the 2 members are `PositionBased` (coordinates reconstruct columns and reading order) and `Stream` (content-stream order, faster but layout-blind).

See [Extract Text & Images](https://ironpdf.com/how-to/extract-text-and-images/) for the extraction surface that consumes this setting.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `TextExtractionMode Enum - IronPDF C# API Reference`
- v2 (human): `TextExtractionMode: IronPDF Text Extraction in C#`
- v3 (balanced): `TextExtractionMode Enum | IronPDF C# Text Extraction`

**Meta-description (120-160 chars)**
- v1 (algorithm): `TextExtractionMode is the IronPDF enumeration for text extraction in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF TextExtractionMode enumeration reference for C#: text extraction modes Determines how text is extracted from PDF documents.`
- v3 (balanced): `TextExtractionMode (Text Extraction) in IronPDF for C#: text extraction modes Determines how text is extracted from PDF documents. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The TextExtractionMode enumeration in IronPDF lives in the IronPdf.Extractions namespace, derived from Enum. Text extraction modes Determines how text is extracted from PDF documents. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.PdfHyphenationLanguage.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `PdfHyphenationLanguage` enumeration in IronPDF selects which TeX hyphenation pattern is applied when `ChromePdfRenderOptions.PdfHyphenation` is enabled.

It belongs to the `IronPdf` namespace and exposes four values: `None` (default), `EnglishUS`, `EnglishBritish`, and `Russian`. The setting only takes effect when CSS leaves word breaks to the renderer.

See [Custom Hyphenation](https://ironpdf.com/how-to/custom-hyphenation/) for justified-text examples.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PdfHyphenationLanguage - IronPDF C# API Reference`
- v2 (human): `PdfHyphenationLanguage: IronPDF Hyphenation in C#`
- v3 (balanced): `PdfHyphenationLanguage Enum | IronPDF C# Layout`

**Meta-description (120-160 chars)**
- v1 (algorithm): `PdfHyphenationLanguage is the IronPDF enumeration for TeX hyphenation patterns in C#. Members: None, EnglishUS, EnglishBritish, Russian.`
- v2 (human): `IronPDF PdfHyphenationLanguage enumeration for C#: selects the TeX hyphenation pattern applied when PdfHyphenation is enabled.`
- v3 (balanced): `PdfHyphenationLanguage in IronPDF for C#: picks the TeX dictionary for justified-text hyphenation in rendered PDFs. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The PdfHyphenationLanguage enumeration in IronPDF lives in the IronPdf namespace, derived from Enum, selecting which TeX hyphenation pattern is applied when ChromePdfRenderOptions.PdfHyphenation is enabled. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

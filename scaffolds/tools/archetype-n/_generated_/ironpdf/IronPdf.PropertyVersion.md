<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.PropertyVersion.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `PropertyVersion` enumeration in IronPDF declares the version stamp written to XMP metadata when an XML payload is embedded into a PDF/A-3 file for ZUGFeRD or Factur-X delivery.

It belongs to the `IronPdf` namespace and exposes four values: `v1` (default), `v1p0`, `v2`, and `v2p0`. The choice should match the schema declared by the paired `SchemaNamespace`.

See [Export PDF/A Format Docs in C#](https://ironpdf.com/how-to/pdfa/) for a full PdfAOptions example.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PropertyVersion Enum - IronPDF C# API Reference`
- v2 (human): `PropertyVersion: IronPDF XMP Metadata Version in C#`
- v3 (balanced): `PropertyVersion Enum | IronPDF C# PDF/A XMP`

**Meta-description (120-160 chars)**
- v1 (algorithm): `PropertyVersion is the IronPDF enumeration for XMP metadata version stamps in C#. Members: v1, v1p0, v2, v2p0 for ZUGFeRD and Factur-X.`
- v2 (human): `IronPDF PropertyVersion enumeration for C#: declares the version stamp written to XMP metadata for PDF/A-3 embedded XML payloads.`
- v3 (balanced): `PropertyVersion (PDF/A XMP) in IronPDF for C#: sets the version recorded for ZUGFeRD or Factur-X embedded XML. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The PropertyVersion enumeration in IronPDF lives in the IronPdf namespace, derived from Enum, declaring the version stamp written to XMP metadata when an XML payload is embedded into a PDF/A-3 file. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.

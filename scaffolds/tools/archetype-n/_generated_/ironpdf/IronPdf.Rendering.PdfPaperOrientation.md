<!--
HUMAN-POLISHED (lite/enum) — programmatic baseline by generate_samples.py, then polished by agent.
Target page: https://ironpdf.com/object-reference/api/IronPdf.Rendering.PdfPaperOrientation.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `PdfPaperOrientation` enumeration in IronPDF sets paper orientation when rendering HTML to PDF.

Part of the `IronPdf.Rendering` namespace, declared values are `Portrait` and `Landscape` (2 total). The value is assigned to `ChromePdfRenderOptions.PaperOrientation` and pairs with `PaperSize` to control the final page geometry before the Chrome renderer rasterizes the HTML.

See [page orientation and rotation](https://ironpdf.com/how-to/page-orientation-rotation/) for typical C# usage.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PdfPaperOrientation Enum - IronPDF C# API Reference`
- v2 (human): `PdfPaperOrientation: IronPDF PDF Rendering in C#`
- v3 (balanced): `PdfPaperOrientation Enum | IronPDF C# PDF Rendering`

**Meta-description (120-160 chars)**
- v1 (algorithm): `PdfPaperOrientation is the IronPDF enumeration for PDF rendering in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF PdfPaperOrientation enumeration reference for C#: paper orientation when rendering HTML to PDF.`
- v3 (balanced): `PdfPaperOrientation (PDF Rendering) in IronPDF for C#: paper orientation when rendering HTML to PDF. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The PdfPaperOrientation enumeration in IronPDF lives in the IronPdf.Rendering namespace, derived from Enum, representing paper orientation when rendering HTML to PDF. Members map to discrete settings consumed by the IronPDF C# rendering pipeline.

**Schema notes**
- Page type: `TechArticle` (lite/enum).
- No FAQPage entries for this variant per spec §5.
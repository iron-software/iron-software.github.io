<!--
GENERATED SAMPLE — Archetype N-Lite (lite/delegate)
Target page: https://ironpdf.com/object-reference/api/IronSoftware.Chrome.ChromeStringDelegate.html

GENERATION NOTES
- Produced by api-overview-seo/work/generate_samples.py from live docfx HTML.
- All API names referenced are pulled directly from the live reference page.
- Prose patterns are templated; expect to need human polish on framing,
  example specificity, and (for full variants) FAQ shape against the page's
  actual class-name long-tail.
-->

## Injected overview (Markdown)

The `ChromeStringDelegate` type is a callback delegate with the signature `void(string)`.

Part of the `IronSoftware.Chrome` namespace, it is consumed internally by the marshaling layer between native and managed code; application code rarely instantiates it directly.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `ChromeStringDelegate - IronPDF C# API Reference`
- v2 (human): `ChromeStringDelegate: IronPDF Chrome Bridge in C#`
- v3 (balanced): `ChromeStringDelegate | IronPDF C# Chrome Bridge`

**Meta-description (120-160 chars)**
- v1 (algorithm): `ChromeStringDelegate is the IronPDF delegate for Chrome bridge in C#. Reference its members, declared values, and usage in PDF generation.`
- v2 (human): `IronPDF ChromeStringDelegate delegate reference for C#: callback delegate for a method containing a single string parameter.`
- v3 (balanced): `ChromeStringDelegate (Chrome Bridge) in IronPDF for C#: callback delegate for a method containing a single string parameter. See members and usage.`

---

## Structured data

**TechArticle abstract**

> The ChromeStringDelegate delegate in IronPDF lives in the IronSoftware.Chrome namespace, derived from MulticastDelegate, representing callback delegate for a method containing a single string parameter. Application code rarely instantiates this delegate directly; the IronPDF runtime wires up invocations across the managed and native boundary.

**Schema notes**
- Page type: `TechArticle` (lite/delegate).
- No FAQPage entries for this variant per spec §5.
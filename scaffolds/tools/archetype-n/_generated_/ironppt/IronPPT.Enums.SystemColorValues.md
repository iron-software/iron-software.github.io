<!--
N-Lite/enum (large). Salient members named, verified 2026-06-23: Window, WindowText, Highlight, HighlightText, ButtonFace, ButtonText, Menu, MenuText. Full set has 29 members.
Target: https://ironsoftware.com/csharp/ppt/object-reference/api/IronPPT.Enums.SystemColorValues.html
-->

## Injected overview (Markdown)

Bind a slide color to an operating-system UI color with `SystemColorValues` in IronPPT, so the rendered element follows the viewer's theme rather than a fixed value. The common picks are `Window` and `WindowText` for surface and body text, `Highlight` and `HighlightText` for selection colors, `ButtonFace` and `ButtonText` for control surfaces, and `Menu` and `MenuText` for menu chrome. Many further entries cover captions, borders, and scrollbars for full OOXML fidelity.

```csharp
var sysColor = SystemColorValues.Window;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `SystemColorValues Enum - IronPPT C# API`
- v2 (human): `SystemColorValues: OS Theme Colors in C#`
- v3 (balanced): `SystemColorValues | IronPPT C# Reference`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Map slide colors to OS theme colors in C# with the IronPPT SystemColorValues enum: Window, Highlight, ButtonFace, Menu, and more.`
- v2 (human): `Bind a slide color to the viewer's system theme in C# with the IronPPT SystemColorValues enum: Window, WindowText, Highlight, and more.`
- v3 (balanced): `Reference for the IronPPT SystemColorValues enum in C#: Window, Highlight, ButtonFace, Menu, and other OS UI colors.`

---

## Structured data

**TechArticle abstract**

> Bind a slide color to an operating-system UI color with IronPPT SystemColorValues so elements follow the viewer's theme. Window and WindowText cover surfaces and text, Highlight covers selection, and further entries cover menus, buttons, and borders in C#.

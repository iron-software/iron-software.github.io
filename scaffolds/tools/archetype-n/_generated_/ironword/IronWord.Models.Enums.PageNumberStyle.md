<!--
N-Lite/enum. Members verified 2026-06-23: Arabic, Roman, None.
No public consumer property found in this API surface; links 0.
Target: IronWord.Models.Enums.PageNumberStyle.html — base type Enum.
-->

## Injected overview (Markdown)

Decide which glyphs a page number uses by choosing a `PageNumberStyle`. `Arabic` produces ordinary 1, 2, 3 numerals and is the common choice for body pages, `Roman` produces i, ii, iii style figures often used for front matter such as a preface or table of contents, and `None` omits the page number entirely.

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PageNumberStyle Enum - IronWord C# Reference`
- v2 (human): `PageNumberStyle: Page Numbering in C#`
- v3 (balanced): `PageNumberStyle Enum | IronWord .NET`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set page numbering in C# with the IronWord PageNumberStyle enum: Arabic for 1, 2, 3 numerals, Roman for i, ii, iii figures, or None to omit.`
- v2 (human): `Choose how IronWord numbers pages in C# with PageNumberStyle: Arabic numerals, Roman figures for front matter, or None.`
- v3 (balanced): `Reference for the IronWord PageNumberStyle enum in C#: Arabic, Roman, and None page numbering values.`

---

## Structured data

**TechArticle abstract**

> Decide which glyphs a page number uses with PageNumberStyle in IronWord. Arabic produces ordinary 1, 2, 3 numerals, Roman produces i, ii, iii figures suited to front matter, and None omits the page number.

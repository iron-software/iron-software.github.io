<!--
N-Lite/enum. Members verified 2026-06-23: Portrait, Landscape.
Consumer verified: PageSetup.Orientation.
Target: IronWord.Models.Enums.PageOrientation.html — base type Enum.
-->

## Injected overview (Markdown)

Set whether a section prints upright or sideways by assigning a `PageOrientation` to `PageSetup.Orientation`. `Portrait` is the default tall layout for ordinary text pages, and `Landscape` rotates the page to a wide layout for tables, charts, or images that need the extra horizontal room.

```csharp
var setup = new PageSetup { Orientation = PageOrientation.Landscape };
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `PageOrientation Enum - IronWord C# Reference`
- v2 (human): `PageOrientation: Portrait or Landscape in C#`
- v3 (balanced): `PageOrientation Enum | IronWord .NET`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set page orientation in C# with the IronWord PageOrientation enum: Portrait or Landscape, applied through PageSetup.Orientation.`
- v2 (human): `Choose Portrait or Landscape layout for a Word section in C# with the IronWord PageOrientation enum, set on PageSetup.Orientation.`
- v3 (balanced): `Reference for the IronWord PageOrientation enum in C#: Portrait and Landscape values for PageSetup.Orientation.`

---

## Structured data

**TechArticle abstract**

> Set whether a section prints upright or sideways with PageOrientation in IronWord, assigned to PageSetup.Orientation. Portrait is the default tall layout, and Landscape rotates the page to a wide layout for tables, charts, or wide images.

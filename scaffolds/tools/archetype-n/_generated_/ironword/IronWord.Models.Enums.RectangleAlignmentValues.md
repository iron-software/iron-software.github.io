<!--
N-Lite/enum. Members verified 2026-06-23: None, Center, Top, Bottom, Left, Right, TopLeft, TopRight, BottomLeft, BottomRight.
Target: https://ironsoftware.com/csharp/word/object-reference/api/IronWord.Models.Enums.RectangleAlignmentValues.html
-->

## Injected overview (Markdown)

Anchor content within its bounding rectangle by picking a `RectangleAlignmentValues` value, which sets the reference point used for placement. `None` applies no explicit anchor, `Center` seats content in the middle, and the edge and corner values (`Top`, `Bottom`, `Left`, `Right`, `TopLeft`, `TopRight`, `BottomLeft`, `BottomRight`) pin it to a side or corner of the rectangle. The [add image guide](https://ironsoftware.com/csharp/word/how-to/add-image/) covers positioning placed objects.

```csharp
var anchor = RectangleAlignmentValues.Center;
```

---

## Recommended metadata

**Meta-title (≤ 60 chars)**
- v1 (algorithm): `RectangleAlignmentValues - IronWord C# API`
- v2 (human): `RectangleAlignmentValues: Anchor Content in C#`
- v3 (balanced): `RectangleAlignmentValues | IronWord C# API`

**Meta-description (120–160 chars)**
- v1 (algorithm): `Set the anchor point within a rectangle in C# with the IronWord RectangleAlignmentValues enum: Center, edges, and corners.`
- v2 (human): `Place content inside its bounding box in C# with the IronWord RectangleAlignmentValues enum: None, Center, edges, corners.`
- v3 (balanced): `Reference for the IronWord RectangleAlignmentValues enum in C#: None, Center, edge, and corner anchor points.`

---

## Structured data

**TechArticle abstract**

> Anchor content within its bounding rectangle in IronWord with the RectangleAlignmentValues enum. None applies no explicit anchor, Center seats content in the middle, and the edge and corner values pin content to a chosen side or corner of the rectangle.
